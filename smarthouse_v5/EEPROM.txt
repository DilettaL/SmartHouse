Cambiamenti della versione precedente:

1) Implementata la scritta >Smarthouse per redere apparente la sincronizzazione giusta
L'eventuale problema di questo cambiamento potrebbe essere il modo con cui la shell prenderebbe l'istruzione, per far fronte a ciò si:
a) inserisce commentato in host_smarthouse.c la printf di smarthouse e si inserisce una readline senza smarthouse
Questo problema si potrebbe ripercuotere anche sulla shell, quindi nel caso:

b)si dovrebbe modificare da riga 163 a 171 smarthouse_shell.c in modo da far si che la stringa in ingresso alla funzione sia solo il nome del comando.

2) Si inseriscono commentate in smarthouse_shell le varie implementazioni delle funzioni

Cambiamenti della seguente versione: IMPLEMENTAZIONE EEPROM

1)Si deve creare un pacchetto per l'utilizzo della eeprom così formato 
DOMANDA PER LUCA: ne creiamo uno o due (uno per la lettura, che usa solo l'host, e uno per la scrittura, che usa solo il firmware)?
//Caso 1 pacchetto
typedef struct EepromPacket
{
	PacketHeader header;
	uint8_t action;		//definisce l'operazione da far fare alle eeprom lettura o scrittura
	uint8_t type;		//definisce se l'operazione va compiuta su un pin analogico o digitale
	uint8_t pin;		//definisce su quale pin effettuare l'operazione
} EepromPacket;
#define EEPROM_PACKET_ID
//Caso 2 pacchetti

typedef enum
{
	digital=0,
	analog=1
}EepromOperations;

typedef struct EeproWritePacket
{
	PacketHeader header;
	EepromOperations type;		//definisce se l'operazione va compiuta su un pin analogico o digitale
	uint8_t pin;		//definisce su quale pin effettuare l'operazione	
}EeproWritePacket;
#define EEPROM_WRITE_PACKET_ID

typedef struct EeproReadPacket
{
	PacketHeader header;
	EepromOperations type;		//definisce se l'operazione va compiuta su un pin analogico o digitale
	uint8_t pin;
	DigitalStatusPacket digital_read;
	AnalogStatusPacket analog_read;
}EeproReadPacket;
#define EEPROM_READ_PACKET_ID

2)Data la creazione del pacchetto è necessario aggiungerlo in tutte le globals

3) LATO FIRMWARE
Implementare le funzioni per la lettura e scrittura
Guardare smarthousev2
4) LATO HOST
Aggiungere nella shell la lettura e scrittura della eeprom
